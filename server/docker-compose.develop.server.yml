version: "3.8"

services:
  app:
    container_name: dialog-app-server
    environment:
      TZ: Asia/Seoul
      JAVA_OPTS: "-Duser.timezone=Asia/Seoul"
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env.develop
