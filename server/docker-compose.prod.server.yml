version: "3.9"

services:
  app:
    container_name: dialog-app-server
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /var/log/dialog:/var/log/spring
    env_file:
      - .env.prod
