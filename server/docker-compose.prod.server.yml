version: "3.9"

services:
  app:
    container_name: dialog-app-server
    environment:
      TZ: Asia/Seoul
      JAVA_OPTS: "-Duser.timezone=Asia/Seoul"
    build: .
    ports:
      - "8080:8080"
    volumes:
      - /var/log/dialog:/var/log/spring
    env_file:
      - .env.prod
